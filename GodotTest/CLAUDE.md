# Стенд на Godot

Проект на Godot 4.7.2 mono, C#. Проверяет то, чего не видно на статичной
картинке: независимость башни, отсутствие «гуляния» оси, поведение при
движении по полю. Конвейер, который делает его спрайты, — в
[корневом CLAUDE.md](../CLAUDE.md).

## Где что лежит

Ниже — запуск, ловушка запуска, управление и правило про корни сцен одной
фразой. Всё остальное разложено по темам и **грузится по ссылке, а не всегда**:

| док | о чём | когда открывать |
|---|---|---|
| [docs/architecture.md](docs/architecture.md) | раскрой каждого файла; что общее у восьми корней, а что принадлежит сцене; `--sprites` и три танка на поле | ищешь, где что живёт, или переносишь код между корнями |
| [docs/terrain.md](docs/terrain.md) | два слота клетки: `Foundation`, `Cover`, состояния, `terrain.json`, кто их читает | правишь то, чем клетка является или чего она стоит |
| [docs/board.md](docs/board.md) | `BoardMap`, пять карт, `Abbey`, `--map-report`, пандусы и уровень моря | правишь карту или пишешь новую |
| [docs/bench.md](docs/bench.md) | `TankTick`, доски `test` и `water`, панель бенча, его флаги | трогаешь `TankBench` или тик танка |
| [docs/wall.md](docs/wall.md) | `Wall.tscn`: `WallKit`/`Fall`/`Stack`/`Bench`, раскладка кладки, физика обвала, тень, `--solved` | как стена построена и как она рушится |
| [docs/wall-bench.md](docs/wall-bench.md) | `WallTankTest`: таран, ворота тарана, HE/AP, секции, тур, `WallProp`, `WallHulls` | танк против стены |
| [docs/water.md](docs/water.md) | брод, ватерлиния по колонкам, бок пруда, пена и кромка, кильватер, гребень, `Ripples` | всё мокрое |
| [docs/props.md](docs/props.md) | тени по солнцу, `Grove`, `Wildfire`, `PropFamily`/`PropTier`, полоса убранства, `WoodBench` | деревья, кусты, камни, пожар в лесу |
| [docs/motion.md](docs/motion.md) | `MovementProfile`, клевок, тряска, дрожание, гусеницы, выхлоп, поворот башни, пожар, обломок, обзор | как танк едет и что при этом шевелится |
| [docs/combat.md](docs/combat.md) | выстрел, отдача ствола и корпуса, тряска камеры, пыль от дульной волны (`ProcKick`), попадание, рикошет (`ProcSpall`), разрыв HE о броню (`ProcSlam`), свет изнутри у пробития (`ProcPierce`), отметина, расклад классов, звук, снаряд, трассер, прицельный луч, **один снаряд в гекс правой кнопкой и таран двойным щелчком** | всё, что стреляет и во что попадает |
| [docs/ui.md](docs/ui.md) | три танка на поле, контактная тень, кольцо выделения, размер класса, боковая панель, `panel/classes/walls.json` | что нарисовано поверх доски и чем это крутят |
| [docs/blast.md](docs/blast.md) | `Effects.tscn`/`EffectsBench`, `SheetBlast` (ввезённый взрыв референса — единственный на доске), воронки, чем делать взрывы, какие типы нужны, что уехало с вычисленным взрывом, два референса и почему пакет частиц не встал | делаешь или правишь взрыв |
| [docs/editor.md](docs/editor.md) | редактор карт: три вкладки, `MapRules`, формат `maps/*.json`, `map-*` | правишь правила карты или сам редактор |
| [docs/selftest.md](docs/selftest.md) | `--selftest`, фильтр по темам, что именно утверждается | добавляешь проверку или разбираешь падение |
| [docs/mcp.md](docs/mcp.md) | Godot-MCP: редакторная и рантайм-половины, десять `bench-*` | агент внутри редактора или внутри стенда |

**Новый раздел пишется в тот док, чей предмет он трогает.** Сюда попадает
только то, что верно до того, как узнал, что трогаешь.

## Запуск

```
dotnet build
"D:\Soft\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64_console.exe" --path "D:\Projects\AgentCoding\BlenderMCP\GodotTest" res://Main.tscn
```

**Путь до движка и `--path` до проекта — всегда абсолютные, и для прогона тестов
это требование.** Относительный `--path GodotTest` верен ровно из корня
репозитория; из любого другого каталога `project.godot` не находится, и Godot
открывает **менеджер проектов** — молча, нулём в коде возврата и без единой
строки вывода. Для `-- --selftest` это худший из исходов: он неотличим от
уведённого `run/main_scene` (см. «Ловушка запуска» ниже) и читается как
«проверки перестали печатать». Абсолютный путь до `.exe` нужен по той же
причине: в `PATH` движка нет.

Доска ABBEY — та же сцена с другой картой, `res://Abbey.tscn` либо
`res://Main.tscn -- --map abbey` (см. [docs/board.md](docs/board.md)). Танковый стенд — своя
сцена: `res://TankTest.tscn`, `res://WaterTankTest.tscn` — он же на воде и
`res://WallTankTest.tscn` — он же против кирпичной стены
(см. [docs/wall-bench.md](docs/wall-bench.md)). Стенд эффектов — две сцены на одной
доске (`BoardMap.Effects`: 5 × 5 на плинте, лес в одном углу, пруд в другом, кольцо
кладки в середине): `res://Effects.tscn` — ввезённый из референса взрыв
(`SheetBlast`, флипбук на 64 кадра; вычисленный снят, см. док). `SPACE`
подрывает, **двойной щелчок по клетке подрывает по ней**, `TAB` открывает панель,
`--fire`/`--at`/`--panel`/`--might` для снимка, снимок панели — с `--ui`
(см. [docs/blast.md](docs/blast.md)).

**Сцену называй позиционным аргументом всегда, а не полагайся на `run/main_scene`.**
Движок переписывает её сам — см. «Ловушка запуска» ниже, — и молчаливый запуск лесного стенда
вместо харнесса выглядит как проверки, переставшие печатать.

`--sprites <TAG>` одевает все три класса в пиксели одного танка — для слоя,
доехавшего до одного раньше остальных; меняются пиксели, а не класс. Разбор — в
[docs/architecture.md](docs/architecture.md).

## Ловушка запуска

**И одна ловушка снаружи кода:** Godot, впервые запущенный другой сборкой движка,
переписывает `project.godot` **и** `TankSpriteTest.csproj` — версию в
`config/features` и в `Godot.NET.Sdk` он ведёт за собой (4.6 → 4.5 при откате на
старую сборку, 4.6 → 4.7 при переходе на новую), а заодно уводит `run/main_scene`
на ту сцену, которую открыли последней. После этого `--selftest` молча запускает
лесной стенд вместо харнесса.

**Версия в этих двух — правка, а не порча**, и откатывать её вслепую значит вернуть
проект на движок, которого на машине нет. Портится ровно одно поле, `run/main_scene`;
называй сцену позиционным аргументом, и оно перестаёт что-либо решать — но
**писать его перестаёт не движок, а только его влияние**: позиционный запуск
`res://Wood.tscn` всё равно переписывает поле на неё. Позиционное имя защищает от
последствия, а не от правки, поэтому `git status` после прогона всё так же
обязателен. Проверяй
`git status` на оба файла после каждого запуска, а третью запись версии —
`NuGet.config` — движок не трогает вовсе, так что её приходится вести руками.

## Корни сцен и правило между ними

Корней восемь: `Main` (харнесс, две сцены), `TankBench` (три), `WoodBench`,
`WallBench`, `ReliefBench`, `Relief3D`, `EffectsBench`, `MapEditor`. Шесть из
них наследуют `SceneRoot`; два рельефных объявлены схематичными и живут своей
арифметикой.

**Ни один разделяемый модуль не знает, какая сцена его запустила.** Проверяется
грепом: кода (не докстрок), обращающегося к `Main.` из чужого файла, ровно два
места — `McpBench` (инструменты MCP водят харнесс) и `SelfTest` (утверждения
харнесса о самом себе). Единственное объявленное исключение между корнями —
`Session`: редактор жмёт Run и меняет сцену на харнесс, харнесс кнопкой
`Edit  F5` возвращает.

Что именно общее, что принадлежит сцене, чего этот заход не тронул и раскрой
каждого файла — [docs/architecture.md](docs/architecture.md).

## Управление

Управление мышью — пять жестов, и все пять решают по тому, что стоит на клетке,
а не по модификатору: **левый клик** по танку берёт его под управление, по пустой
клетке — приказ ехать; **правый клик** по любой клетке — один снаряд в неё (одно
нажатие — один снаряд; стоит корпус — работает броня, пусто — воронка), по своей
клетке — отмена; **двойной левый клик** по танку — таран (приказ ехать с умыслом,
удар на трети последней ноги, мнёт оба корпуса по классам); **СКМ щелчком** —
уничтожить танк под курсором либо поджечь лес на клетке; **СКМ протяжкой** —
панорама, колесо — зум. Постоянный приказ «стрелять по этому танку, пока не сказано
иное» остался у панели, `--attack` и `bench-attack`
(см. [docs/combat.md](docs/combat.md)).

Клавиши: `F` переключает режим башни, `A`/`D` корпус, `Q`/`E` башня, `W`/`S` шаг
вперёд-назад, `1`…`3` танк, `SPACE` крутит башню, `M` прицеливание мышью, `X`
рисует крест на оси, `Z` выстрел, `V` источник вспышки, `P` клевок, `B` тряска,
`I` дрожание корпуса, `N` рыскание башни, `K` стабилизатор башни, `L` отдача
только у башни, `O` выхлоп, `C` ход гусениц, `[` отдача ствола, `]` сдвиг
спрайта от отдачи (`RecoilShear`), `\` звук, `J` поджигает танк, `U` попадание
(чем заряжено — ряд панели либо `--ammo`; умолчание HE, то есть разрыв на плите,
а рикошет надо запросить), `Y` калибр, `H`/`T`/`G` гасят слои, `ESC` отмена
приказа, `F12` скриншот, `R` сброс, `TAB` боковая панель со всем этим же в виде
списков, слайдеров и кнопок. Уничтожение под курсором — это детонация БК
(`ProcRack`).

**Буквы кончились, и `[` — следствие этого, а не выбор.** A–Z заняты все, плюс
`SPACE`, `TAB`, `ESC`, `F12` и диапазон `1`…`9`; `[` была первой свободной
клавишей вне существующих диапазонов, и скобка хотя бы показывает, куда уходит
ствол. За ней ушли `]` и `\`, то есть свободных одиночных клавиш больше нет:
следующему переключателю понадобится разбор с модификатором.
