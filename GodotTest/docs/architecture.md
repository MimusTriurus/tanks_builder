# Устройство стенда: файлы и границы между сценами

Часть [GodotTest/CLAUDE.md](../CLAUDE.md) — стенда на Godot. Там запуск,
управление и правило про корни одной фразой; здесь раскрой каждого файла и
разбор того, что общее у восьми корней, а что принадлежит сцене.

## Раскрой файлов

`SceneRoot.cs` — общая база всех корневых сцен: пять флагов про сам прогон
(`--capture`, `--capture-at`, `--zoom`, `--ui`/`--no-ui`), один скриншот на
четверых и жест средней кнопки — протяжка панорамирует, щелчок делает то, что у
корня своё (см. ниже),
`FrameClock.cs` — шаг кадра под `--capture`, `Angles.cs` — `Mod`, `WrapAngle` и
снап пеленга к стороне гекса, `Ordnance.cs` — список калибров и глубина каждого,
`Footing.cs` — где у танка ноги на доске (точка контакта, патч, высота двух
концов корпуса на ступени, шов, вдоль которого стенд режет глубину),
`AtlasSet.cs` грузит атласы, `HexField.cs` держит поле и всю гексовую
арифметику, `Terrain.cs` — два слота клетки (`Foundation` — пол, `Cover` — что на
нём стоит, `CoverState` — что с ним стало) и таблица, чего каждый из них стоит,
из `terrain.json` (см. [docs/terrain.md](terrain.md)), `Vehicle.cs` — один танк на поле (спрайт, атлас, класс, клетка, все
его снаряды в воздухе, где у него срез ствола, три его приказа — кого атаковать,
в какой гекс положить один снаряд, кого таранить — и все
его часы), `Gunnery.cs` — можно ли отсюда выстрелить в того, кому что достаётся
при таране, `Shell.cs` — снаряд в полёте,
`SelectionRing.cs` — кольцо на земле под управляемым (и второе, красное, под
целью),
`TankSprite.cs` рисует два слоя, `EffectLayer.cs` — отрендеренные
слои эффектов (вспышка, дым, выхлоп, пожар), `MovementProfile.cs` задаёт ход и
размер по классам,
`BodyPitch.cs`, `BodyRumble.cs` и `EngineTremble.cs` — эффекты движения,
`ExhaustLoop.cs` — часы выхлопа, `BurnLoop.cs` — часы пожара,
`HitLoop.cs` — часы попадания, `TrackLoop.cs` — часы гусеницы,
`RecoilLoop.cs` — часы ствола,
`TurretScan.cs` — обзор на стоянке, `ControlPanel.cs` — боковая панель,
`PanelText.cs` — её имена, описания, диапазоны и умолчания из `panel.json`
(панель танкового бенча читает тем же классом свой `bench.json`, панель стенда
эффектов — свой `effects.json`, и в нём умолчаний нет вовсе: их место — текст
шейдера),
`ClassConfig.cs` — пер-классовые тройки из `classes.json`,
`WallConfig.cs` — форма каждой стены (сид, колонки, ряды, грани, слои) из
`walls.json`,
`Main.cs` — управление и разбор флагов, `SelfTest.cs` — проверки,
`TankTick.cs` — **что кадр делает с танком**: ход по приказу, посадка на клетку,
все его часы и **снаряд** — как он уходит, летит и садится, — в одном объекте;
вынут из `Main` целиком, чтобы харнесс и танковый стенд водили танк одним кодом,
а не двумя (см. [docs/bench.md](bench.md)),
`TankBench.cs` — сцены `TankTest.tscn`, `WaterTankTest.tscn` и
`WallTankTest.tscn`: один танк с панелью со всеми его настройками, на полосе
7 × 4 с горкой, прудом и островом, либо на 12 × 12 воды с розеткой из семи
наземных гексов, либо на гексагоне ровной земли с кольцом кладки вокруг танка
и четырьмя стенами-образцами, `TankBench.Tour.cs` — тур `--ram-tour`, где он
таранит их все по очереди и печатает, что каждый удар сделал с каждой гранью
порознь
(см. [docs/bench.md](bench.md) и [docs/wall-bench.md](wall-bench.md)),
`ProcBlast.cs` — **общий текст всех взрывов на доске, а не взрыв**. Был взрывом
HE в грунте (три квада, шесть семей, 62 униформы), проиграл сравнение ввезённому
и снят целиком; файл остался потому, что его текст четыре других эффекта
компилируют внутрь себя: `DustInk` (что такое облако земли — профиль, рваность,
упаковка, ступенчатый свет) берут `ProcKick`, `ProcSlam`, `ProcSpall`, `ProcRack`;
`FrameCode` (квад, посадка, `blast_throw`, `blast_footing`) — они же и
`SheetBlast`; `RingCode` с готовым шейдером — `ProcRack`; `Quad`, `Uniform` и
`Colour` — то, чем проверка спрашивает шейдер, с чем он скомпилирован. **Удалить
файл нельзя — четыре эффекта не соберутся** (см. [docs/blast.md](blast.md)),
`ProcKick.cs` — пыль, сбитая дульной волной с земли, по референсу стреляющего
танка: бледное выбитое ядро у среза, масса, ушедшая по каналу и тормозящая,
юбка по земле, бурая завеса с угольками. **Два квада на той же пыли, что у
взрыва** — общий текст `ProcBlast.DustInk`, — своего у него только где стоят
элементы и какого они цвета (две пары стопов, смешанные средним возрастом
массы). Поднимается `Stage3D.Kick` — третий пул на шесть, — а зовёт его
`TankTick.Kicked` с каждого выстрела, вне зависимости от того, попал он или нет,
и воронки не копает: под срезом ямы не бывает
(см. [docs/combat.md](combat.md)),
`ProcSpall.cs` — **рикошет: снаряд ударил в броню и не прошёл**. Вспышка на
плите, веер раскалённых осколков по **отражению** прилетевшего от нормали плиты
(`2 * outward - from`, обе половины уже измерены — `AtlasSet.HitBearing` и
`Shell.Bearing`), сам снаряд, ушедший в сторону, серая пыль с плиты и искры,
доехавшие до земли. **Два квада на той же пыли, что у взрыва и у дульной волны**;
своего — где стоят элементы и что пыль тут краска и грунтовка, а не земля.
Поднимается `Stage3D.Spall` — четвёртый пул на шесть, — а зовёт его
`TankTick.Bounced` **вместо** отрендеренного `burst_atlas`, когда
`Gunnery.Penetration` дал ноль: две картины прилёта на один снаряд — двойная
модель, а ноль это тот же порог, по которому уже выбирается звук рикошета и
считаются убийства. Воронки не копает, `--spall off` возвращает старую картинку
(см. [docs/combat.md](combat.md)),
`ProcSlam.cs` — **разрыв HE о броню: снаряд остановился и не отскочил**. Ядро
подрыва о металл, шар огня, скатывающийся с плиты, масса сажи и горелой краски,
осколки, которые **падают**, пролив по борту до гусениц и волна, идущая по самой
плите. Направление — **нормаль плиты** (`Vehicle.Blown` — это `Vehicle.Graze` с
вычитанием, снятым с него): снаряд, который остановился, никуда не уходит, и азимут
прилёта выпадает из модели целиком. Поэтому родня ему взрыв в грунте, а не рикошет:
картинку несёт `ProcBlast.DustInk`, а `blend_add` — только короткое яркое начало,
и **ни одного «ушедшего снаряда» тут нет**, что и есть половина отличия. Кадр
удара — общий с рикошетом (`ProcSpall.SpallFrame`): куда смотрит поверхность,
сколько пережило проекцию, пол под охватом и конус, раскрывающийся когда охват
закрывается. Поднимается `Stage3D.Slam` — пятый пул на шесть, — а зовёт его
`TankTick.Blasted`, и вилка спрашивает **сначала боеприпас**: снаряд, который
рвётся, не отскакивает, поэтому HE рвётся на плите пробил он её или нет, а отскок
остался свойством AP. Урон не тронут — `Gunnery.Penetration` про начинку не знает.
Отметину пишет `DamageTo` выше вилки, поэтому она достаётся всем трём картинам.
`--ammo he|ap` выбирает, `--slam off` возвращает старую.
**Поверхностей три, и все три — один текст**: `Surface.Armour`, `.Masonry`
(известковая пыль, вспышка вместо шара, масса уходит **вдоль** стены, обломки не
рисуются — их кидает `WallRig.Burst`) и `.Pierced` (AP: огня ноль, пыли две
трети). C# пишет два числа, каждый кирпичный член в шейдере — `mix` против нуля,
поэтому броня осталась той арифметикой, что судилась глазами. По кладке зовёт его
`TankBench.Landing`, и **вместо** земляного конуса: выстрел в стену поднимал взрыв
на земле, что и было сообщено как ошибка (см. [docs/combat.md](combat.md)),
`ProcRack.cs` — **детонация БК: танк перестал быть танком**. Вспышка заряда в
кольце погона, семь струй из погона и люков, шар, тёмная голова сажи, пыль
из-под полок и кольца по земле (текст воронки, `ProcBlast.RingCode`, целиком).
**Пеленга в модели нет вовсе** — газам из пробитого корпуса всё равно, куда
смотрел корпус, — поэтому кадр удара двух других эффектов тут нарочно не
подключён, а `Bounds` нечего обходить. Посадка бесплатна: `anchor_px` и есть
погон, так что горизонталь — ноль по построению, а вертикаль —
`AtlasSet.HeightSpanPx`. Размер — `Sprite.BodyScale` **убитого**, а не
`Ordnance` стрелявшего. Поднимается `Stage3D.Rack` — шестой пул на шесть, — а
зовёт его `TankTick.Detonated` из `Kill`, то есть единственный эффект на доске
не на триггере снаряда. **Работа его — шов**: `Wreck.Blaze` отвечал единицей на
кадре гибели, теперь встаёт за `RiseSeconds` (= `CharSeconds`), и длинный столб
принадлежит обломку, а не этому слою. Смертей две, решает плита убившего
попадания (`rear` — топливо), ответ хранит `Wreck.Racked`. Тряска камеры прямо
вверх, второго толчка в лес и второго звука нет — их посылает `Kill`.
Копоть под обломком кладёт стенд в
карту пепла (`Stage3D.Scald`, по состоянию доски, на любую смерть; воронки нет —
корпус сам себе крышка). Подмена позы — срез в один кадр (другие слои атласа,
промежуточного кадра нет) и стоит он **на взрыве**: задержка под густую сажу была
построена и снята — прятала втрое больше пикселей, а читалась двумя событиями
вместо одного. `--rack off` не рисует ничего, рампу и копоть не забирает
(см. [docs/blast.md](blast.md)),
`ProcPierce.cs` — **свет изнутри: снаряд прошёл броню**. Ядро в пробоине,
просвечивающее сквозь обшивку, и свет по шву погона. **Единственный слой, который
к картинке добавляется, а не заменяет её**: отрендеренная пара — это уже наружная
сторона пробития (`ProcSlam` своей докстрокой), никем не нарисована была
внутренняя. И единственный, для которого довод «на холсте» остался верен: альфа
корпуса есть только там, на доске этого не нарисовать. **Маска — то, что
рисуется**: слой рисует свой кадр корпуса своим материалом, поэтому `COLOR.a` и
есть силуэт; `TEXTURE` не читается. Часов своих нет — у попадания уже есть
(`HitLoop`, экранные кадры), плюс два поля: `Through` и `Elapsed`. Рисуется
раньше башни, иначе шва не видно. `--pierce off` оставляет пару в одиночестве
(см. [docs/combat.md](combat.md)),
`Craters.cs` — что выстрелы разрыли: точки, а не клетки; состояние доски,
рисует стенд, `PitArt.cs` — как воронка нарисована: своя плоскость на земле, яма
со сходящимися лучами и освещённой дальней стенкой, выброс с пальцами и брызгами,
губа над валом; швы между слоями — полосой, а не линией (`soft`), камни сделаны
и убраны: на сотне пикселей камень в пять пикселей не может быть ни освещён, ни
огранён. **Не отметина в карте пепла** — та 8
единиц на тексель (воронка в ней пять текселей) и умеет только затемнять, а
воронка это тёмная яма посреди выброса; глубина нарисована, потому что под этой
камерой опущенная вниз геометрия похоронена своей же клеткой
(см. [docs/blast.md](blast.md)),
`SheetBlast.cs` — **ввезённый взрыв референса, и единственный на доске** (сцена
`Effects.tscn`): 64 кадра
просчитанного клуба дыма со знаковой нормалью на каждом
(`assets/ExplosionVFX/`, MIT), разложенные по `MultiMesh`, модель которого C#
переписывает каждый кадр; частицы, затухание о землю, свет и glow пакета
заменены — GPU-частицу нельзя поставить на кадр, буфера глубины в
`gl_compatibility` нет, света на доске нет вовсе. Форма — по референс-кадру:
радиальная половина броска гаснет, вертикальная идёт дальше (столб, а не шар),
доля клубов брошена как **шипы** — быстрее, тоньше и растянуты по своему полёту, —
огонь гаснет и по времени, и по высоте, мелкая фракция лежит юбкой по земле.
Событие в три акта: вспышка, один выброс с насыщающимся подъёмом, долгое оседание
(крупное падает, пыль обвисает); разброс — в длительности клубов, а не в их
рождении, потому что взрыв выбрасывает один раз.
Поднимается `Stage3D.Boom` — пул
на шесть, копает воронку и берёт **размер** (`might`): одно число на весь взрыв,
масштабом на трансформе от посадки, и воронка растёт с ним. **Он же и есть взрыв
доски**: снаряд, ушедший в поле, идёт через `Stage3D.Land` — один метод на оба
корня, — и выбора там больше нет, `BlastSource` с флагом `--burst` снят вместе с
вычисленным взрывом. В игре размер даёт `Ordnance.At(TankTick.Calibre)` — чем
заряжено, тем и рвёт.
**И он же — взрыв на воде (`water_he`), своего файла у того нет**: тот же
флипбук с одной подменённой рампой (`SheetBlast.Wet` → `FoamStops`) — белый
вместо коричневой земли, — из **своего**, третьего пула на шесть, потому что
рампа вшита в материал при постройке. Воронки не копает вовсе, а вместо
нарисованного кольца бьёт по полю ряби втрое сильнее обычного удара
(`Stage3D.Waves`, `Ripples.Strike`). Четыре написанных руками столба воды
выброшены, чтобы это выяснить: вещество решает цвет и не решает больше ничего, а
закрутку и самозатенение просчитанных кадров сумма эллипсов не даёт. `--spout off`
возвращает земляной взрыв — то же облако, коричневое, с воронкой
(см. [docs/blast.md](blast.md)),
`EffectsBench.cs` / `EffectsBench.Panel.cs` / `EffectsBench.Sheet.cs` — сцена
`Effects.tscn`: доска `BoardMap.Effects` (5 × 5 на плинте, лес, пруд, кольцо
кладки),
`SPACE`, двойной щелчок, readout (перекрестия нет — стенд не рисует на канве
ничего) и боковая панель на 33 ручки в
пяти группах — четыре `sheet.*` и отметина (`effects.json` — подписи и диапазоны,
значения читаются из текста шейдера и из полей модели); танков нет
(см. [docs/blast.md](blast.md)),
`WoodBench.cs` — отдельная сцена `Wood.tscn`: один гекс, по одному дереву
каждого вида и средняя кнопка, без танков, звука и панели
(см. [docs/props.md](props.md)),
`WallKit.cs` / `WallFall.cs` / `WallStack.cs` / `WallBench.cs` — сцена
`Wall.tscn`: кирпичная стена, сгенерированная и обрушенная кодом, без Blender
(см. [docs/wall.md](wall.md)), `WallProp.cs` — стена, стоящая на названной клетке названной доски:
план, рисунок, солвер и единственный переход board↔prop, общий у обоих стендов,
`WallHulls.cs` — коллайдеры солвера, нарисованные рамками поверх картинки:
стоящая кладка, отпущенная и таранный ящик тремя цветами
(`--hulls`, см. [docs/wall-bench.md](wall-bench.md)),
`MapEditor.cs` — **редактор карт**, сцена `Editor.tscn`: доска на стенде, три
вкладки кистей, живой аудит на каждый мазок. Восьмой корень, и он же
единственный экземпляр авторинга доски — `BoardMap` отказывает второму
авторингу, а не второму корню. Метки доезжают до 3D через хук
`HexField.Ink`, который `Stage3D.Signature` уже сворачивает в пересборку
(см. [docs/editor.md](editor.md)),
`MapEditor.Panels.cs` — **две панели**: слева вкладка и кисть, справа выбранная
клетка по группам. Ни одна не вторая модель: каждый контрол читает `MapEdit` и
пишет через его же охраняемые методы, поэтому отказ доезжает до строки состояния
так же, как отказ мазка. Клавиши остались и остались единственным, что слушает
`_UnhandledInput`; кнопки зовут тот же код, а `Brushes()` — тот один список, по
которому идут и палитра, и `Q` (см. [docs/editor.md](editor.md)),
`Masonry.cs` — **что карта говорит о кладке на клетке**: шесть граней маской и
четыре числа формы. Маска — истина, `Run()` — вывод: правила спрашивают про
грань, а `WallKit` берёт азимут и число сторон, потому что стена — одна
сложенная цепь кирпичей, а не N стен рядом. Две несмежных дуги — не одна стена,
и это `wall.run`, нефатальный fault, а не отказ кисти. `Laying()` —
единственный стык с `WallKit`: что клетка промолчала, берёт рецепт стенда,
что назвала — пишется поверх. По нему `MapEditor.Bricks` ставит кирпичи, и
это первое на проекте, что кладёт стену с карты; солвера в редакторе нет —
толкать её нечем,
`Session.cs` — **что один корень передаёт следующему через смену сцены**: имя
доски и то, редактор ли прислал. Единственная связь двух корней в проекте, где
правило «ни один разделяемый модуль не знает, какая сцена его запустила», —
собрана в одном файле нарочно: Run в редакторе сохраняет доску и меняет сцену на
`Main.tscn`, кнопка `Edit  F5` там возвращает. Статик, потому что переход не
сохраняет больше ничего; передача **побеждает** `--map`, потому что аргументы
командной строки переход переживают и флаг запуска к моменту возврата устарел
(см. [docs/editor.md](editor.md)),
`Parking.cs` — **спавн: клетка плюс класс, которого доска хочет на ней**. Класс —
предпочтение, а не роспись: кто на доске, решает загрузка атласов, а класс говорит
куда этот танк поедет; `Pair()` спаривает в два прохода (сперва названные, потом
по порядку) и потому чистая — где встанет танк без парковки, знает грунт, а не
она. Спавн при этом две работы, и танк из них одна: вторая — то, из чего
`MapRules.ReachFaults` меряет доску (см. [docs/board.md](board.md)),
`MapRules.cs` — **правила доски одним экземпляром**: что не так с пандусами,
водой, спавнами и достижимостью, списком, а не броском. Написаны они были
трижды — guard'ы поля, аудитор и самопроверка, — и аудитор однажды пережил
guard, который перефразировал. Список, а не бросок, потому что доска под
кистью редактора невалидна почти всегда; guard'ы бросают первый элемент этого
же списка (см. [docs/editor.md](editor.md)),
`MapFile.cs` — доска в `maps/*.json` и обратно: JSON контейнером, картинка
внутри; декодер один и это `BoardMap.FromGround`. Круговым прогоном оплачен
второй экземпляр алфавита — единственный в файле,
`MapEdit.cs` — **доска под кистью**: буквенная сетка, кисти трёх вкладок,
отмена снимком на мазок. Godot в нём нет вовсе, поэтому кисти судятся без
сцены. Состояние — буквы, а не оси: алфавит не декартово произведение, и
кисть, оставившая бы клетку без буквы, отказывает и говорит почему
(см. [docs/editor.md](editor.md)),
`BoardMap.cs` / `BoardMap.Abbey.cs` — какая доска: размер, что в каждой клетке,
где спавны, плюс аудитор авторской карты; сцена `Abbey.tscn` — это тот же
`Main` с другой картой (см. [docs/board.md](board.md)).

Четыре файла раскрой этот пропускал, и все четыре живые:
`ClimbLean.cs` — **плоскость под танком, спружиненная**, и две разные вещи,
выведенные из неё: клевок корпуса и форма контактной тени; пружинится земля, а
не рисунок, потому что тень лежит **в** грани и сдвигается вместе с ней, а из
угла этот сдвиг не восстановить — угол на двух курсах из шести ноль, а плоскость
нет. Спрашивают шесть файлов (`Vehicle`, `TankSprite`, `TankTick`,
`EffectLayer`, `HexField`, `SelfTest`);
`FlashSheet.cs` — лист дульной вспышки, шестнадцать кадров четыре на четыре,
вспышка вдоль +x; грузится абсолютным путём, как атласы, и по той же причине:
перекеил лист — следующий прогон его подхватил. Ждёт уже кеингованный файл
(`Fire_rgba.png`), три ловушки подъёма белой бумаги в RGBA — при
`key_on_white` в [../../docs/tools.md](../../docs/tools.md);
`ReliefCap.cs` — земля, что стоит **перед** танком и **выше** его,
перерисованная поверх него: узел на танк, а не z-индекс на клетку, потому что
правило здесь два сравнения, а индекс одно число; общий у харнесса, `Stage3D`,
`HexField`, `Vehicle` и `Relief3D`;
`MapEditor.Menu.cs` — меню редактора: новая доска, открыть, сохранить,
сохранить как — и единственный guard между ними; диалоги вместо ещё трёх
клавиш, потому что это три операции, которым нужно слово автора
(см. [docs/editor.md](editor.md)).


## Что общее, а что принадлежит сцене

Корней восемь — `Main` (харнесс, две сцены), `TankBench` (три), `WoodBench`,
`WallBench`, `ReliefBench`, `Relief3D`, `EffectsBench`, `MapEditor`, — и правило между ними
одно: **ни один разделяемый модуль не знает, какая сцена его запустила**. Проверяется это грепом:
кода (не докстрок), обращающегося к `Main.` из чужого файла, осталось два места, и
оба названы ниже.

**Одно исключение теперь есть, и оно объявлено: `Session`.** Редактор жмёт Run и
меняет сцену на `Main.tscn`, харнесс кнопкой `Edit` возвращает — значит два корня
знают друг о друге по пути сцены. Состояние этой связи собрано в `Session.cs`, а сам переход — два
`ChangeSceneToFile` с именем сцены-соседа (`Main.cs`, `MapEditor.cs`) и больше
нигде: правило про **разделяемые модули** не нарушено ни разу, нарушено правило
про корни, между ровно двумя из них и ради теста-драйва. Альтернатива была пустить
танк внутрь сцены редактора — второй харнесс внутри инструмента, которым харнесс
судят (см. [docs/editor.md](editor.md)).

**Снаряд принадлежит танку, а не доске, и это последнее, что пришлось забрать у
харнесса.** Список рождённых им снарядов лежит на `Vehicle.Rounds`, полёт, прилёт
и уборку ведёт `TankTick.Fly`, а `TankTick.Fire` кончается самим выстрелом.
Довод — тот же, которым существует `TankTick`: пока список жил в `Main`,
одно-танковый стенд не мог получить снаряд иначе как второй копией всего полёта,
и `Z` там поднимала одну вспышку. **Узлы при этом не дети танка** — снаряд не
должен ехать на корпусе, который его выпустил, а на 3D-сцене спрайт переселён в
собственный рендер-таргет, и трассер, привязанный туда, рисуется внутри картинки
танка вместо доски. Отсюда `TankTick.Deck`: доска и никогда не танк.

**Шов между «чей снаряд» и «в кого он» — `TankTick.Launch`, хук по образцу
`Aim`.** В кого стрелять — утверждение о **двоих**: чья полоса, чья броня, не
стоит ли третий на линии; это харнесса и там и осталось (`Main.RoundFor`). Что
снаряд делает дальше — утверждение об **одном**, и оно здесь. Хук не отвечен —
выстрел уходит в землю, и на бенче так и происходит.

**Механика отделена по построению: бенчи её собирают, а не переписывают.**
`TankBench` водит танк тем же `TankTick`, что харнесс; `WoodBench` жжёт лес тем
же `Wildfire` и той же `Grove`; `WallBench` рушит стену тем же `WallRig`. Бенч,
переписавший то, что судит, есть бенч, меряющий сам себя, — и две копии согласны
до первой правки в одной из них.

**Пять флагов про сам прогон живут на `SceneRoot`**, а не по копии на корень:
`--capture`, `--capture-at`, `--zoom`, `--ui`/`--no-ui` и скриншот, который они
существуют ради. Их было четыре копии, различавшихся словом в печатаемой строке;
почти всякое измерение в этом проекте — попиксельная разница двух снимков,
поэтому корень, чей захват уехал от остальных, тихо мерил бы другое.

**Счётчик кадров при этом нарочно не общий.** Харнесс стреляет на
`++frames > CaptureAt`, бенчи на `frame >= CaptureAt`, а стена печатает строку
дрейфа ровно на кадре. Свести их в одно правило значит сдвинуть каждый эталонный
снимок в репозитории на кадр — то есть правку картинки в одежде рефакторинга.

**Данные доски — `BoardMap`, все четыре.** Сетки харнессной доски лежали на
`Main`, и `BoardMap.Bench` читал их оттуда: то единственное, чем карта является,
находилось внутри сцены, которая её рисует. `SelfTest` и `Relief3D` спрашивают
теперь `BoardMap.Bench.LevelsFor(columns, rows)` — карту, спроецированную на поле
другого размера, — вместо трёх декодеров харнесса.

**Что осталось зависеть от `Main`, и почему это верно.** `McpBench` ходит в
`Main.Live` — инструменты MCP водят харнесс, и на доске без него отвечают
«no bench». `SelfTest` читает `Main.FlaggedRows`, `Main.McpOnByDefault`, `Main.RamContact`
и два пороговых числа (`SunkAt`, `StepsAt`): это утверждения харнесса о
самом себе, и тест — то место, где их и проверяют. (`Main.ShallowAt` рядом с
ними объявлена, но читает её только сам харнесс.)

**Чего этот заход не тронул, и это названо, а не забыто.** `ReliefBench` держит
собственные `Squash`, `Lift`, `LevelAt`, `FlatCentre`, `FlatCellAt` — вторую
копию гексовой арифметики с высотой, — и стреляет `--shot`/`--shot-at` вместо
`--capture`. Первое следует из того, чем он объявлен («схематично нарочно»: у
него свои полигоны и свои высоты, а не доска), второе — переименование
задокументированного запуска, то есть правка, которую делают нарочно, а не
мимоходом. И панель `TankBench` остаётся своим набором строк: файл `panel.json`
сверяется с панелью харнесса в обе стороны, поэтому запись про строку, которую
строит только бенч, провалила бы ту проверку. Подписи и умолчания бенча живут
в своём `bench.json`, тем же механизмом и со своей сверкой — см.
[docs/bench.md](bench.md).

**Циферблаты выстрела уехали к нему: `TankTick.Calibre` и `TankTick.HitSide`.**
Оба были написаны и в `Main`, и в `TankBench` — калибр это чем пушка заряжена,
сторона попадания это откуда придёт **следующий** ручной снаряд, — и обе теперь
одно поле, которое каждый корень читает и пишет своим рядом панели и своей
клавишей. Циферблат в двух местах расходится на первой правке в одном из них, и
это была не гипотеза: пара уже жила двумя объявлениями.

**`ApplySize` остаётся на обоих корнях, и это не долг, а видимость пары.** У
харнесса она про троих, у бенча про одного, общего у них — одна строка
`MovementProfile.Size`, которая и так общая.

**Жест средней кнопки живёт на базе** — `SceneRoot.MiddleTapped`. Одна кнопка
делает две вещи, а какую именно, решает жест: протяжка панорамирует, щелчок
делает то, что у корня своё. Четыре корня писали ради этого одни и те же четыре
строки — запомнить нажатие, забрать его на отпускании, спросить `MiddleTap`, — и
четыре копии это четыре шанса на то, что одна из них решит на **нажатии**, где
два жеста ещё неразличимы.

**И одна мерка на три проекции: `Vehicle.Bore`.** Где у танка срез и куда смотрит
канал, было выписано трижды — в снаряде (доска), в луче (собственная картинка
танка) и в земляном выстреле (только направление). Проекция правда принадлежит
вызывающему; **мерка нет**, и три копии согласны ровно до того дня, когда кто-то
поменяет, как меряется пушка.

## Спрайты взаймы и три танка на поле

Флаг `--sprites <TAG>` и почему на доске стоят ровно `LTP`, `MTP`, `HTP`.
Сам запуск и управление — в [CLAUDE.md](../CLAUDE.md).

**`Main.SharedSpriteDir` = `null`, и это его нормальное состояние**, а задаётся
он теперь флагом **`--sprites <TAG>`**, а не правкой константы. Случай, ради
которого он есть, — слой, доехавший до одного танка раньше остальных, — это ровно
тот случай, когда «поправь исходник и пересобери» неверная форма работы: судят
картинку, а снять её дважды не должно требовать правки между снимками.

**Сейчас в этом состоянии стенд не находится** — все трое перерендерены и на 24
курсах, и с направленной тенью, — но приём остаётся тем, ради которого флаг есть:
`--sprites MTP` одевает всех троих в его пиксели, и новый слой видно на поле, а не
на контактном листе. У каждого из
трёх классов теперь свой атлас, поэтому каждый носит свои пиксели, и на поле стоят
три танка, а не один трижды.

Флаг оставлен ради случая, ради которого им уже пользовались дважды: новый слой
доехал до одного танка раньше остальных, и смотреть его надо на поле, а не на
листе. Поставленный в тег, он заставляет все три класса читать атласы того танка.
Меняются **пиксели, а не класс** — `AtlasSet.Tag` остаётся тем, о чём просили,
поэтому `MovementProfile` по-прежнему даёт каждому свою скорость, разгон и размер.
Это делает проверку размера **строже** настоящей: три одинаковых спрайта при 0.85,
1.00 и 1.15 не могут различаться ничем, кроме масштаба. Зато всё остальное на поле
становится одним танком, нарисованным трижды, — и в этом вся цена.
`AtlasSet.Load(root, tag, dir)` — третий параметр именно за этим.

**Чей танк одалживает пиксели — тот, на который смотрят.** Тяжёлый заслужил свой
заход из-за пушки: у него не тонкий ствол, а короткая толстая гаубица, и на
отвёрнутых курсах труба вырождается в обрубок 27×19 px. Это худший случай для
`AtlasSet.FindMuzzles`, ровно тот, ради которого у неё есть запасной проход без
эрозии, — и `--selftest` на этих пикселях проходит целиком.

**Три танка, по одному на класс: `LTP`, `MTP`, `HTP` — все разобранные.**
Односетевые `HT`, `MT` и `LT` из стенда убраны. Причина одна и она не про
структуру файла, а про экран: у них лента не отдельный меш, поэтому ходить она не
может вовсе, а танк, скользящий на мёртвых гусеницах, — не эталон для сравнения, а
старая ошибка, всё ещё висящая на виду. Атласы их уехали в `Sprites/Obsolete/`;
ничто их больше не грузит, вернуть можно строкой в `MovementProfile.Tags`.

Убрано в четырёх местах, и все четыре стоит знать: `MovementProfile.Tags`, три профиля в
`MovementProfile` (перетегованы на `LTP`/`MTP`/`HTP` — профиль всегда был про
класс, а не про то, как разрезана сцена), фолбэк в `For()` и упоминания в
проверках. Разбор клавиш чинить не пришлось: он давно ходит диапазоном
`>= Key.Key1 and <= Key.Key9` с зажимом по числу загруженных, а не перечислением,
которое ломалось на каждом добавленном танке.
