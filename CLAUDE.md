# BlenderMCP — конвейер танковых спрайтов

Превращает сгенерированные ИИ модели танков (ComfyUI → glTF) в послойные
изометрические атласы спрайтов для 2D-игры на гексагональной сетке. Корпус и
башня рендерятся отдельными слоями, которые игра накладывает друг на друга, —
поэтому башня может целиться в одну сторону, пока корпус едет в другую.

Всё работает внутри Blender: либо из его Text Editor (`Alt+P`), либо через мост
Blender MCP. Скрипты — обычные модули: импортируй и вызови точку входа, либо
запусти как `__main__`, тогда возьмётся его `CONFIG` как есть.

## Раскладка

```
Input3D/     исходные .glb от генератора, по одному на танк
Images/      референсные рендеры танков; Environment/ — арт клетки,
             Terrains/ — виды земли, Water/ — лист воды и петля пруда
Scenes/      рабочие .blend: HT/MT/LT (односетевые) и *_PARTS (по частям),
             MT_PARTS_1 — канонический средний; плюс WALL_scene и Hexes
Sprites/     результат, папка на сцену:
             <TAG>/{hull,turret,hex,...}_atlas.{png,json}
             Живых наборов три: LTP, MTP, HTP. Obsolete/ — прошлые прогоны
out/         черновой вывод разовых прогонов
GodotTest/   стенд на Godot 4.7 mono (C#): восемь сцен-корней и свой CLAUDE.md
docs/        разбор конвейера по подсистемам, по ссылке (см. ниже)
```

Полная раскладка, с обоснованием каждого каталога и каждой конвенции имён, —
[docs/assets.md](docs/assets.md).

В сцене **старого образца** лежит `world` (empty), под ним `Hull` и `Turret`,
плюс `Camera` и `Light`. Так устроены HT, MT и LT: они пришли одним мешом и
разделены руками. Разобранные сцены собраны иначе, и это канон для всех
следующих — [docs/tank-scene.md](docs/tank-scene.md). `Hex` строится в момент
рендера и в файл не сохраняется — это верно для любой сцены.

## Конвейер

1. **Импорт** `.glb` в чистую сцену.
2. **Разделение** корпуса и башни — на разобранной модели уже сделано
   генератором; на односетевой `split_turret.py` ([tools.md](docs/tools.md)) либо вручную.
   **Гусеницы отделяет `track_split.py`**, и тоже сам: лента не двусмысленна,
   она многочисленна, поэтому меряется, а не размечается.
3. **Выделение геометрии под эффекты** — вручную, по разу на сцену. Одна и та
   же операция, разные куски: `Barrel` (срез ствола, дочерний к корню слоя
   башни) даёт выстрел, `Engine` (выпускная жалюзи или патрубок, дочерний к
   корню слоя корпуса) даёт **выхлоп, пламя горящего танка и его дым** — один
   штамп на три слоя. Ни то, ни другое не обязательно — чего нет, то
   пропускается. Порядок действий — [MUZZLE_FLASH.md](MUZZLE_FLASH.md).
   **Попаданию в броню и следу от него руки не нужны вовсе** — плиты меряются
   лучом один раз, см. `hit_point.py` в [effects.md](docs/effects.md), и по одному
   штампу работают оба слоя.
4. **Поиск оси вращения** — `turret_axis.py`. Штампует `ring_axis` на корпус и
   башню; на односетевой сцене ещё и переносит origin башни.
5. **Построение тайла** — `hex_base.py` на односетевой сцене,
   `parts_render.ground_tile` на разобранной, по центру на этой оси.
6. **Рендер** — `render_set` из `sprite_atlas.py`, одно задание на все слои.
7. **Проверка** композитом, глазами по картинке.

Шаги 4–7 делаются одним вызовом `tank_pipeline.run()`, **на обеих структурах**.
Ось он мерит сам, но только если её ещё нет: штамп на месте — значит его,
возможно, ставили руками, и переписывать его молча нельзя.


## Где что лежит

Этот файл — то, что верно **до того, как узнал, что трогаешь**: раскладка
коротким деревом, порядок конвейера, конвенции, правила проверки и ловушки
самого Blender. Всё остальное вынесено и **грузится по ссылке, а не всегда**.

| док | о чём | когда открывать |
|---|---|---|
| [docs/assets.md](docs/assets.md) | раскладка целиком; чем является то, что отдаёт генератор — суп из оболочек, отсутствие скрытых поверхностей, отсутствие погона | ищешь ассет или режешь модель |
| [docs/tank-scene.md](docs/tank-scene.md) | каноничная структура танка по частям: имена, родители, трансформы, материалы, ловушка иерархии | авторишь сцену нового танка |
| [docs/tools.md](docs/tools.md) | `tank_parts`, `turret_axis`, `sprite_atlas`, `atlas_pack`, `hex_base`, `split_turret`, `track_split`, `assemble_tank`, `key_on_white`, `water_sheet`, `stamp_ports`, `stamp_bore` — и пять ловушек измерения | чем меряют, режут и рендерят |
| [docs/effects.md](docs/effects.md) | дульная вспышка, выхлоп, пожар танка и его столб, попадание в броню, след от него | ставишь или правишь эффект на танке |
| [docs/layers.md](docs/layers.md) | `sprite_height`, `ground_shadow`, `barrel_recoil`, `draw_order`, `wreck_pose`, `track_cycle`/`parts_render`, `turret_ring` | слои, которые двигаются относительно корпуса |
| [docs/pipeline.md](docs/pipeline.md) | `tank_pipeline.run()`, задание `render_set`, что проверяется, листы проверки, числа по трём танкам, память атласов | гоняешь конвейер целиком или считаешь байты |
| [docs/brick-wall.md](docs/brick-wall.md) | `brick_wall.py`: раскладка кладки, цвет замером, зазор, физика, `fit_to_hex` | кирпичная стена в Blender |

Стенд на Godot — своя половина, и она грузится **по каталогу**:

| док | о чём | когда открывать |
|---|---|---|
| [GodotTest/CLAUDE.md](GodotTest/CLAUDE.md) | запуск, управление, правило про корни — и свой указатель по темам | грузится сам, когда трогаешь `GodotTest/` |

Рядом, как и раньше:

- [MUZZLE_FLASH.md](MUZZLE_FLASH.md) — как делается вспышка выстрела, целиком
- [HEX_TILES.md](HEX_TILES.md) — как моделируются многоуровневые тайлы земли
- [HEX_TILES_3D.md](HEX_TILES_3D.md) — пандусы и вода на стенде: карта, правило прохода, брод, ватерлиния

**Новый раздел пишется в тот док, чей предмет он трогает.** Сюда попадает
только то, что верно про весь проект **и** нужно до того, как узнал, что
трогаешь; иначе через полгода этот файл снова станет тем, чем был.

## Конвенции

- **Фронт — это −Y**, то есть `front_dir = 270`. Это объявленная конвенция
  импорта, а не измерение. Подтверждай её на каждой модели по нулевому кадру
  рендера: единственное настоящее свидетельство в геометрии — фары и лобовой
  лист, а читать их нужно глазами. Все три текущих танка ей подчиняются.
- **Плоскотоповый гекс**, азимут 0, наклон 30°, **24 шага**. Кадры чередуются:
  в сторону гекса и в угол, по шесть тех и других.
- **Азимут обязан быть кратен 30 для гекса** (45 для квадрата). Экранные
  координаты плоской фигуры — это она же, повёрнутая на −азимут и сжатая по
  вертикали в `sin(наклон)` раз; мимо оси симметрии тайл проецируется косо и
  перестаёт читаться как клетка сетки.
- **`steps` кратно 6** для гекса, чтобы направления попадали на направления
  сетки.
- **Шагов 24, а не 12, и это единственная ручка плавности поворота.** Атлас
  квантует курс, поэтому шаг сетки кадров и есть минимальное движение, которое
  танк способен показать: 15° против 30°.

  **Удвоить только корпус и башню нельзя**, хотя просят обычно это. Ленты и
  гусеничные катки приварены к корпусу, ствол к башне, отметины к плитам, плюм и
  пожар к моторной палубе: слой, шагающий по 30° под корпусом, шагающим по 15°,
  **едет относительно него**. Хуже всего отметинам — это жёсткие декали на броне
  в 60 px от якоря, где полшага это 8 px сползания по плите.

  Цена замерена по самим атласам, а не прикинута: 156 МБ в памяти на три танка,
  44.8 на диске. Байты по слоям и два оставшихся рычага —
  [docs/pipeline.md](docs/pipeline.md).
- Корпус, башня и тайл **делят одну ось вращения** — погон. Именно это позволяет
  класть спрайты друг на друга в точке `anchor_px`.

## Правила проверки

- **Никогда не рапортовать успех по возвращаемому значению.** Сложить слои без
  офсетов, записать PNG и посмотреть картинку. Неверный азимут или ось вращения
  там очевидны и совершенно невидимы в числах. `_check_composite.png` в каждой
  выходной папке — это и есть та проверка; включай в неё кадры с башней,
  развёрнутой относительно корпуса, ради этого послойность и затевалась.
- После разделения: `exact_partition`, башня сохранила пушку (по bbox), гусеницы
  по-прежнему стоят на z=0.
- После переноса origin: мировой bbox не сдвинулся.
- Ничто не касается кромки тайла (`max_edge_alpha == 0`).
- **Самопроверка стенда: по ходу работы — фильтром своей темы
  (`--selftest wall`), перед коммитом — целиком.** Фильтр отвечает на «покажи мою
  тему», а не на «всё ли цело», и фильтрует он отчёт, а не работу; полный прогон
  стоит одиннадцать секунд. Разбор —
  [GodotTest/docs/selftest.md](GodotTest/docs/selftest.md).

## Ловушки самого Blender

Пять ловушек измерения этих ассетов — в [docs/tools.md](docs/tools.md), ловушка
иерархии сцены — в [docs/tank-scene.md](docs/tank-scene.md). Здесь то, что
кусает любой `bpy`-код в этом проекте.

- **`me.vertices.foreach_set("select", ...)` тихо не делает ничего.** В нынешнем
  Blender `select` — лениво создаваемый атрибут `.select_vert`, запись
  промахивается, и следующий `bpy.ops.mesh.delete` работает по состоянию «всё
  выделено» после импорта — так была снесена целая башня. Работай через bmesh и
  сверяй, что число удалённых равно числу выделенных.
- **`bpy.ops.object.origin_set` не работает через MCP** — в контексте моста нет
  `selected_objects`. Переноси origin сдвигом вершин на −delta и матрицы на
  +delta, они сокращаются точно.

## Окружение

- Blender **5.2 LTS**, установлен из Microsoft Store. Его `blender.exe` лежит
  под `WindowsApps` и **не запускается напрямую** — мешают ACL, а алиас
  `blender-launcher.exe` отсоединяется. Поэтому `--background` недоступен, и
  MCP-инструменты `*_for_cli` падают по той же причине. Управляй живым
  экземпляром, переключая сцены через `bpy.ops.wm.open_mainfile` (сперва проверь
  `bpy.data.is_dirty`).
- Движок рендера — **EEVEE**. Набор из 12 кадров по 256 px в три слоя считается
  за секунды.
- Меши — от 0.4 до 1.5 млн вершин. Работай numpy поверх `foreach_get`, никаких
  питоновских циклов по вершинам.
- Для стенда нужен .NET SDK 8 (стоят 6, 8, 9). Godot лежит в
  `D:\Soft\Godot_v4.7.2-stable_mono_win64\`; версия `_console.exe` печатает
  вывод в терминал, обычная — нет, поэтому для `--selftest` нужна именно она.
- **Версия движка записана в проекте трижды, и чинятся эти три записи по-разному.**
  `config/features` в `project.godot` и `Godot.NET.Sdk` в `TankSpriteTest.csproj`
  редактор переписывает сам под ту сборку, которой проект открыли; `NuGet.config`,
  указывающий на `GodotSharp/Tools/nupkgs` этой же сборки, он не трогает никогда.
  Значит пара расходится молча, а сборка продолжает идти из глобальной папки
  пакетов — и заговорит `NU1301` только на чистой машине, то есть там, где
  подстраховаться нечем. Отсюда и то, что доказать этот путь здесь **нельзя**:
  4.7.2 уже лежит в `~/.nuget/packages`, поэтому восстановление проходит и с
  заведомо неверным путём в конфиге. Проверяются они втроём и глазами.
